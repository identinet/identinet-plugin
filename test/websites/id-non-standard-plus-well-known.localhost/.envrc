# Documentation: https://direnv.net/man/direnv-stdlib.1.html
source_up_if_exists
dotenv_if_exists

# export DID_KEY=$(didkit key-to-did -k key.jwk)
# export KEY_ID=$(echo "${DID_KEY}" | sed -ne 's/^did:key://p')
# export VERIFICATION_METHOD=$(jq -r '.assertionMethod[0]' < did.json)
